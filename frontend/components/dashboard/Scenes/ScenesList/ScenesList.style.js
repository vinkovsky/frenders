import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles({
    progress: {
        top: `calc(50% - 25px)`,
        left: `calc(50% - 25px)`,
        position: 'absolute'
    }
});